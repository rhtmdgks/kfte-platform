/**
 * 고정 헤더 아래 내부 페이지 여백.
 * 헤더 높이 = clamp(3.75rem, 3.4rem+1vw, 6rem) + 콘텐츠 간격 1.25rem
 */
export const pageMainClassName =
  "min-h-screen bg-background pt-[calc(clamp(3.75rem,3.4rem+1vw,6rem)+1.25rem)] pb-20 md:pb-28"
